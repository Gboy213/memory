# Этап 04 — Живая вёрстка, UX и дизайн-система

Read-only на проде: GET/навигация, без форм. URL — из карт репо.

1. Открой основные экраны браузерным инструментом (Playwright MCP); недоступен —
   curl + статический анализ, отметь деградацию.
2. Console/network ошибки, 404 ресурсов, битые ссылки.
3. Дизайн-система: прод против канона (токены, kit); инлайн-стили и «свои» CSS-правила
   там, где есть примитив канона — привести к канону (сам).
4. Неиспользуемые CSS-правила, классы, токены: `rg` по всем static/html/js, 0 вхождений —
   удалить одним коммитом-списком (разрешено контрактом), сторожа зелёные.
5. Неавторизованный GET к API отдаёт redirect/401, не данные.

Пустые состояния, «некрасиво», перекомпоновка экранов — не находки без воспроизводимого дефекта.
